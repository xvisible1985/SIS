package strategy

import "context"

// startReconcileLoop periodically checks for missed fills and re-places stale orders.
// This is a stub — full implementation is in a future task.
func (ar *AccountRunner) startReconcileLoop(ctx context.Context) {
	// TODO: implement reconcile loop
}
